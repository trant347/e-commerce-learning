; semantic-ui-react@2.1.5 has not updated its peerDependencies to include
; React 19 yet, even though it works at runtime with the findDOMNode shim
; in polyfills/findDOMNode.js. Until upstream publishes a React 19-aware
; release, allow npm to ignore that one peer conflict.
legacy-peer-deps=true
